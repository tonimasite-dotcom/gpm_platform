# Промпт для продолжения работы над GPM Platform

Скопируй всё содержимое этого файла первым сообщением в новый чат.

---

Продолжаем работу над проектом **GPM Platform** (Flutter web/Android +
FastAPI backend + PostgreSQL, интеграция с CRM).

**Первым делом прочитай файл `PROJECT_HANDOFF_2026-09-09_CABINET_FIXES.md`
в корне репозитория — это полный снапшот состояния на 09.09.2026.**
Также подтянется память проекта (`gpm_platform_state`,
`gpm_platform_cabinet_backlog`, `gpm_platform_deploy_notes`,
`gpm_platform_handoff_convention`).

## Контекст в двух словах

- `main` = `origin/main` = **`a4a3dd6`**, дерево чистое, всё задеплоено в
  production (`app.gpmbot.ru` / `app-api.gpmbot.ru`).
- За прошлую сессию (09.09) провели аудит трёх кабинетов (клиент, логист,
  исполнитель) и выпустили три батча правок: общий модуль статусов/дат,
  фиксы в списках/деталях заказов, форма создания заказа (гражданство
  «Только РФ / Любое», обязательное «Описание работ», два поля веса),
  разбивка сумм в финансах исполнителя, причёсанная «Главная»
  исполнителя, бэкенд-фикс начисления (ставка, не клиентская цена),
  удалена плашка в чатах.
- `gh` CLI установлен и авторизован (`tonimasite-dotcom`), деплой:
  `gh workflow run deploy-production.yml --repo tonimasite-dotcom/gpm_platform --ref main -f target=<frontend|backend|all>`.
- `DESIGN_FREEZE.md` действует (кроме уже согласованной карточки финансов).

## Что нужно от тебя в первом ответе

1. Сделай read-only проверку: `git status`, `git log --oneline -6`,
   `gh auth status`. Назови фактические branch / HEAD и подтверди, что
   production на `a4a3dd6`.
2. Спроси у меня **результат ручной приёмки батча 3** (раздел 5 хендоффа):
   плашка в чатах убрана, новые поля в форме создания, разбивка в
   финансах, вид «Главной» исполнителя.
3. Ничего не коммить и не деплоить, пока я не подтвержу задачу.

## Что делаем дальше (бэклог, раздел 6 хендоффа)

**Заблокировано — жду моих ответов:**
- **P0-3** — оценка исполнителя «С нареканиями». Нужны: (A1) величина
  изменения рейтинга для нового значения `complaint`; (A2) делать ли
  серверный эндпоинт записи оценки сейчас или сначала demo + ярлык.

**Согласованные отдельные задачи:**
- Серверный сквозной счётчик номеров заказов `APP-NNNNNN` (бэкенд
  миграция + счётчик, `target=all`).
- P2-18 — чистка экрана деталей заказа (состав/порядок полей у
  исполнителя и логиста), обсудить со мной состав.

**Открыто, контекста достаточно:** P1-8 (форма создания не грузит
тип/контакты клиента в API-режиме), P1-10 (пропадает отклонённый отклик),
P1-11 (гонка фильтра гражданства), P2 (подтверждения деструктивных
действий, очередь модерации во вкладке профиля логиста, мёртвая кнопка
поддержки у клиента), P3 (рейтинг обрезается до целого, валидация полей
профиля клиента, мёртвый legacy-код `lib/client|worker|logist`).

Спроси меня, с чего начинаем.
