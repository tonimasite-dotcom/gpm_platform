Продолжаем работу над GPM platform: Flutter web/Android, FastAPI и PostgreSQL,
репозиторий `tonimasite-dotcom/gpm_platform`.

Сначала полностью прочитай файл
`PROJECT_HANDOFF_2026-08-31_CRM_LIVE_DRAFT_EDITOR_PENDING.md` в корне
репозитория. Это главный источник истины по сессии 31.08.2026. Затем сделай
read-only проверку `git status`, текущей ветки, HEAD и `origin/main`.

Коротко:

- production и `main` сейчас на `7024d76`: CRM ownership/city visibility уже
  выпущена; номера со слешем и определение города из CRM-адреса исправлены;
- CRM-заявку видит и публикует только назначенный логист компании GPM; другой
  логист её не видит; частные логисты создают заявки только в приложении;
- исполнители видят опубликованные заявки только по своим городам;
- адрес работ скрывается от исполнителя до назначения — это ожидаемо;
- ветка `feature/edit-order-drafts` на `b3f85df` добавляет редактирование `NEW`
  перед публикацией для владельца-клиента, обычного логиста и назначенного
  CRM-логиста;
- `b3f85df` отправлен в origin, локальные backend/Flutter-проверки и CI run
  `33382173657` зелёные;
- редактор ещё не в `main` и не в production;
- не сливай и не деплой редактор без отдельного явного подтверждения владельца;
- для его production-релиза нужен `target=all`;
- регистрация логистов отложена;
- локальный Chrome запускай строго на порту `8090` из-за CORS;
- не выводи secrets, env, токены, SSH-ключи, пароли и приглашения.

После проверки кратко сообщи фактическое состояние и дождись следующего запроса.
