# Prompt for continuing the GPM project in a new chat

Copy everything below into the new chat.

---

Продолжаем работу над проектом GPM из существующего локального репозитория:

```text
C:\Users\Юра\Desktop\gpm_platform\gpm_platform
```

GitHub:

```text
https://github.com/tonimasite-dotcom/gpm_platform
```

Сначала полностью прочитай актуальный handoff:

```text
PROJECT_HANDOFF_2026-08-21_AUTODEPLOY.md
```

Для старой инфраструктурной истории при необходимости используй:

```text
PROJECT_HANDOFF_2026-08-18_PROD_SWITCH.md
PRODUCTION_DEPLOYMENT.md
```

Не начинай работу заново и не повторяй уже завершённые ручные деплои. Сначала
выполни только read-only проверку `git status`, текущей ветки, `origin/main` и
последних коммитов, затем кратко подтверди мне фактическое состояние.

Текущая точка:

- frontend: https://app.gpmbot.ru
- backend: https://app-api.gpmbot.ru
- health: https://app-api.gpmbot.ru/health
- первый автоматический production run `32490148967` завершился успешно;
- production base: `e595d15` плюс более новый коммит со снапшотом, если он уже
  находится в `main`;
- production теперь публикуется вручную через GitHub Actions workflow
  `Deploy production`, target `all/backend/frontend`;
- environment `production` ограничен веткой `main`;
- GitHub environment secrets уже настроены: `PROD_SSH_PRIVATE_KEY` и
  `PROD_SSH_KNOWN_HOSTS`;
- ZIP и MobaXterm больше не являются штатным способом деплоя;
- мобильный двухшаговый экран ролей и входа опубликован;
- роли: Клиент (`client`), Исполнитель (`worker`), Логист (`logist`);
- временный тестовый вход для всех ролей — `admin/admin`;
- настоящая регистрация ещё не реализована;
- backend работает через PostgreSQL;
- защищённый endpoint подсказок адреса реализован, но провайдер не активирован;
- текущий код рассчитан на DaData, `DADATA_API_KEY` в production не задан;
- ручной ввод адреса работает как fallback;
- я отдельно спросил, не должно ли это работать через Яндекс, поэтому не
  активируй DaData и не меняй провайдера без моего подтверждения.

Ближайшие открытые задачи:

1. Сначала обсудить и выбрать провайдера подсказок адреса: DaData или Яндекс.
2. После выбора безопасно настроить backend-ключ и проверить автоподсказки от
   ввода до сохранения координат.
3. Затем спроектировать настоящую регистрацию и раздельные аккаунты вместо
   общего `admin/admin`.
4. Технический долг CI: проверить актуальную официальную версию
   `actions/checkout` и убрать предупреждение Node.js; сделать backend health
   retry без лишнего JSON traceback.
5. Позже усилить SSH-деплой отдельным ограниченным пользователем вместо root.

Правила работы:

- Отвечай по-русски, короткими понятными шагами.
- Не проси и не показывай в чате секреты, приватные ключи, токены или содержимое
  production env.
- Не добавляй секреты в Git или Flutter `.env`.
- Для изменений создавай отдельную ветку, проверяй diff и тесты, добавляй в
  коммит только относящиеся к задаче файлы.
- Не запускай production workflow без моего явного запроса на публикацию.
- Если я прошу опубликовать, используй GitHub Actions `Deploy production`, а не
  ручной ZIP.
- При ошибке сначала изучи Actions step/logs; MobaXterm используй только если
  удалённая диагностика действительно необходима.
- Не удаляй резервные копии и локальную пару deploy-ключей без отдельного
  подтверждения точных путей.

После чтения handoff скажи:

1. на каком коммите и ветке сейчас репозиторий;
2. что уже работает онлайн;
3. что именно осталось нерешённым;
4. какой следующий безопасный шаг ты предлагаешь.

---
