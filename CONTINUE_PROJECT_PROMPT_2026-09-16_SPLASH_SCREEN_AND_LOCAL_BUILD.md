# Промпт для продолжения работы над GPM Platform

Скопируй всё содержимое этого файла первым сообщением в новый чат.

---

Продолжаем работу над проектом **GPM Platform** (Flutter web/Android +
FastAPI backend + PostgreSQL, интеграция с CRM).

**Первым делом прочитай файл
`PROJECT_HANDOFF_2026-09-16_SPLASH_SCREEN_AND_LOCAL_BUILD.md` в корне
репозитория — это полный снапшот состояния на 16.09.2026.** Также
подтянется память проекта (`gpm_platform_state`,
`gpm_platform_cabinet_backlog`, `gpm_platform_deploy_notes`,
`gpm_platform_handoff_convention`).

## Контекст в двух словах

- `main` = `origin/main` = **`8d66543`**, дерево чистое.
- Сессия 16.09 занималась **только** дизайном Android-сплэш-экрана (по
  просьбе — «больше ничего не трогай») и решением проблемы локальной
  Android-сборки на этой машине (Windows-профиль с кириллицей ломал
  `flutter build apk`). Бэклог кабинетов, мессенджер, CRM-ветки — не
  трогались, всё по состоянию на 11.09.
- Сплэш-экран: исправлена системная заглушка Android 12+ (обрезанный
  логотип на синем фоне) — добавлены `values-v31/styles.xml`,
  adaptive-icon для сплэша, перегенерированные иконки с полями, бежевый
  фон `#EDE9E4` как на экране входа. Только `android/app/src/main/res/**`,
  10 файлов, коммит `8d66543`. **Эту правку НЕ нужно деплоить** через
  `deploy-production.yml` — она не затрагивает веб-сборку, которую катит
  этот workflow. Продакшен веб-части по-прежнему на `c0f0b53` — правку
  увидит только тот, кто поставит новый APK на телефон.
- Локальная Android-сборка теперь работает: `subst G:` на папку проекта +
  `PUB_CACHE`/`GRADLE_USER_HOME` на пути без кириллицы + `pub get` и
  `build` строго в одной команде (иначе что-то в среде откатывает
  `.flutter-plugins-dependencies` обратно на путь с кириллицей между
  вызовами). Точные команды — в памяти `gpm_platform_deploy_notes` и в
  разделе 4.2 хендоффа.
- Оба APK уже собраны и лежат в
  `build/app/outputs/flutter-apk/app-debug.apk` (146.6 МБ) и
  `app-release.apk` (51.3 МБ, отдан пользователю для установки на
  телефон вручную). **Release подписан debug-ключом** —
  `android/app/build.gradle` всё ещё содержит `TODO: Add your own signing
  config for the release build`; для Google Play нужен отдельный keystore
  (новая задача, раздел 6.4 хендоффа, не начата).
- `gh` CLI установлен и авторизован (`tonimasite-dotcom`), деплой:
  `gh workflow run deploy-production.yml --repo tonimasite-dotcom/gpm_platform --ref main -f target=<frontend|backend|all>`.
- Ветку `feature/crm-logist-city-visibility` **по-прежнему не мержить** —
  устарела на ~6000 строк, функциональность уже в main с 28.08 другими
  хешами. Подробности — раздел 7 хендоффа.
- `DESIGN_FREEZE.md` действует (кроме уже согласованных исключений:
  карточка финансов исполнителя, сплэш-экран).

## Что нужно от тебя в первом ответе

1. Сделай read-only проверку: `git status`, `git log --oneline -6`,
   `gh auth status`. Назови фактические branch/HEAD (`8d66543`) и
   подтверди, что веб-продакшен по-прежнему на `c0f0b53` (это нормально,
   не рассинхрон — см. выше).
2. Спроси у меня результат ручной приёмки — накопился чек-лист из 6
   пунктов (раздел 5 хендоффа): номер заказа `APP-NNNNNN`, баннер в
   переписке убран, метка гражданства «Любое»/«Только РФ», плюс
   неподтверждённые с 09.09 пункты батча 3 (форма создания заказа,
   разбивка в финансах, вид «Главной» исполнителя), плюс — сплэш-экран на
   реальном телефоне после установки `app-release.apk`.
3. Напомни про **P0-3** (оценка «с нареканиями», ждёт A1/A2) и
   **мессенджер v2** (ждёт двух ответов по дизайну) — оставлены как есть,
   не начинай без прямого запроса, но не забывай про них.
4. Ничего не коммить и не деплоить, пока я не подтвержу задачу.

## Что делаем дальше (бэклог, раздел 6.3 хендоффа) — порядок решён 11.09,
## без изменений

1. **P1-11** — гонка фильтра гражданства до кэша профиля исполнителя.
2. **P1-10** — отклонённый отклик исполнителя пропадает из всех вкладок.
3. **P1-8** — форма создания заказа не грузит тип/контакты клиента в
   API-режиме (блокирует и «логист создаёт заказ за клиента»).
4. **P2-18** — чистка экрана деталей заказа. Нужен мой ввод по составу
   полей — спроси, когда дойдём.
5. **P2 (прочее)** — подтверждения деструктивных действий, очередь
   модерации во вкладке профиля логиста, мёртвая кнопка поддержки у
   клиента, скрывать обработанные отклики у логиста.
6. **P3** — рейтинг обрезается до целого, валидация полей профиля
   клиента, мёртвый legacy-код `lib/client|worker|logist`.

Отдельно, если я попрошу вернуться к Android: **раздел 6.4 хендоффа** —
настройка релизного keystore для Google Play (сейчас release-сборка на
debug-ключе). Не начинай без запроса.

Спроси меня, с чего начинаем.
