Продолжаем GPM Platform: Flutter web/Android, FastAPI, PostgreSQL и CRM.
Репозиторий: `tonimasite-dotcom/gpm_platform`.

Первым делом полностью прочитай главный master-снапшот:

```text
PROJECT_HANDOFF_2026-09-02_FULL_PROJECT_SNAPSHOT.md
```

Он объединяет архитектуру, роли, CRM, безопасность, инфраструктуру и все
релизы до точки остановки. Старые handoff-файлы используй только как историю.

После чтения выполни только read-only проверку:

```text
git status --short --branch
git log --oneline --decorate -10
git rev-parse HEAD
git rev-parse main
git rev-parse origin/main
```

Ожидается чистый синхронизированный `main`. HEAD может быть документационным
коммитом новее `1a2a258`. Production backend/frontend работают из
функционального commit:

```text
8d7e283d93e1ab87ace43a0181959594c716a510
```

Успешные workflow:

```text
CI #39:          33629007587
Demo #49:        33629255446
Production #13:  33629518564, attempt #2, target=all
Docs demo #50:   33637335612
```

В production уже находятся досрочное закрытие набора, единый чат заявки,
исправление chat IDs `001/26`, заголовок по CRM-номеру, скрытие внутреннего
счётчика после отклика и server-side фильтрация `РФ / Не РФ`.

Не повторяй commit, merge или deploy. Точка продолжения — ручная проверка после
hard refresh на синтетических аккаунтах:

1. для `001/26` у исполнителя один чат `Заявка № 001/26`;
2. чат открывается без `Bad state: Not Found`;
3. исполнитель и назначенный логист видят сообщения друг друга;
4. support не создаёт второй чат;
5. после отклика исполнитель видит «Набор исполнителей завершен», не `1/2`;
6. RF/non-RF заявки видны только подходящему гражданству и городу;
7. досрочное закрытие сохраняет assigned worker и блокирует новые отклики.

Пользователь уже подтвердил базовый сценарий `001/26`: назначенный логист видит
заявку, редактирует и публикует её, исполнитель нужного региона откликается.

При дефекте сначала запиши роль, order ID, экран и точный текст. Не удаляй
старые chat rows и не меняй production вручную. Исправление делай новой feature
от актуального `main`, с регрессией и отдельным подтверждением deploy.

Соблюдай `DESIGN_FREEZE.md`. Не включай legacy Telegram. Используй только
синтетические данные. Не выводи secrets, populated env, пароли, токены,
invitation codes, SSH keys и Authorization headers.

В первом ответе кратко назови фактические branch/HEAD, production `8d7e283` и
попроси результат первой ручной проверки: открывается ли единственный чат
`Заявка № 001/26` без ошибки.
