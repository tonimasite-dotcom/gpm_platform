<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ has its own system splash screen API that overrides the plain
         windowBackground drawable used on older versions. Without these attributes the
         OS synthesizes its own splash from the launcher icon and crops it into a circle
         (this is what produced the blue background with the cropped logo). -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
