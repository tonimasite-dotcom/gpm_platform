# Промпт для продолжения работы над GPM Platform

Скопируй всё содержимое этого файла первым сообщением в новый чат.

---

Продолжаем работу над проектом **GPM Platform** (Flutter web/Android +
FastAPI backend + PostgreSQL, интеграция с CRM).

**Первым делом прочитай файл
`PROJECT_HANDOFF_2026-09-11_ORDER_NUMBER_AND_CRM_BRANCH_CLEANUP.md` в корне
репозитория — это полный снапшот состояния на 11.09.2026.** Также
подтянется память проекта (`gpm_platform_state`,
`gpm_platform_cabinet_backlog`, `gpm_platform_deploy_notes`,
`gpm_platform_handoff_convention`).

## Контекст в двух словах

- `main` = `origin/main` = **`c0f0b53`**, дерево чистое, всё задеплоено в
  production (`app.gpmbot.ru` / `app-api.gpmbot.ru`, health зелёный).
- Сессия 11.09 (батч 4): сервер теперь сам присваивает номера заказов
  (`APP-NNNNNN`, счётчик в БД, атомарное присвоение); убран отдельный
  красный баннер «Единый рабочий чат…» внутри переписки (не тот, что
  убрали в батче 3 — тот был на списке чатов, этот — внутри конкретного
  чата); надпись «Гражданство исполнителя: Не важно» у логиста и
  исполнителя исправлена на «Любое» (и «Только РФ» / «Только не РФ»),
  чтобы совпадать с формой создания заказа.
- **Важно:** ветку `feature/crm-logist-city-visibility` **не мержили** —
  расследование показало, что эта функциональность уже в production с
  28.08 (влита покоммитно, другими хешами), а сама ветка с тех пор
  устарела на ~6000 строк diff (нет мессенджера, редактора черновиков и
  т.д.). Мерж откатил бы недели работы. Подробности — раздел 7 хендоффа.
  Если где-то всплывёт старая память «CRM ownership не в main» — это
  устаревшее утверждение, не доверять.
- `gh` CLI установлен и авторизован (`tonimasite-dotcom`), деплой:
  `gh workflow run deploy-production.yml --repo tonimasite-dotcom/gpm_platform --ref main -f target=<frontend|backend|all>`.
- `DESIGN_FREEZE.md` действует (кроме уже согласованной карточки финансов
  исполнителя).

## Что нужно от тебя в первом ответе

1. Сделай read-only проверку: `git status`, `git log --oneline -6`,
   `gh auth status`. Назови фактические branch / HEAD и подтверди, что
   production на `c0f0b53`.
2. Спроси у меня **результат ручной приёмки батча 4** (раздел 5 хендоффа):
   номер заказа в формате `APP-NNNNNN`, баннер в переписке убран, метка
   гражданства верная. Заодно уточни пункты батча 3, по которым ещё не
   было явного подтверждения (форма создания заказа, разбивка в финансах,
   вид «Главной» исполнителя).
3. Напомни про **P0-3** (оценка «с нареканиями», ждёт A1/A2) и
   **мессенджер v2** (ждёт двух ответов по дизайну) — они сознательно
   оставлены как есть, не начинай их без прямого запроса, но не забывай
   про них.
4. Ничего не коммить и не деплоить, пока я не подтвержу задачу.

## Что делаем дальше (бэклог, раздел 6.3 хендоффа) — порядок решён 11.09

1. **P1-11** — гонка фильтра гражданства до кэша профиля исполнителя.
2. **P1-10** — отклонённый отклик исполнителя пропадает из всех вкладок.
3. **P1-8** — форма создания заказа не грузит тип/контакты клиента в
   API-режиме (блокирует и «логист создаёт заказ за клиента»).
4. **P2-18** — чистка экрана деталей заказа. Нужен мой ввод по составу
   полей — спроси, когда дойдём.
5. **P2 (прочее)** — подтверждения деструктивных действий, очередь
   модерации во вкладке профиля логиста, мёртвая кнопка поддержки у
   клиента, скрывать обработанные отклики у логиста.
6. **P3** — рейтинг обрезается до целого, валидация полей профиля
   клиента, мёртвый legacy-код `lib/client|worker|logist`.

Спроси меня, с чего начинаем.
